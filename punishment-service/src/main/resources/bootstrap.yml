server:
  port: 2222
spring:
  application:
    name: punishmentservice
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        service-id: config-server
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/